'use client';

export function JustdialIcon({ className = '' }) {
  return (
    <div className={`inline-flex items-center font-black tracking-tight select-none font-sans text-xl ${className}`}>
      <span className="text-[#0073E6]">Just</span>
      <span className="text-[#FF6F00]">dial</span>
    </div>
  );
}
